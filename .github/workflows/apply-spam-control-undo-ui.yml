name: Apply Spam Control Undo UI Patch

on:
  push:
    branches:
      - feature/spam-families
    paths:
      - '.github/scripts/apply_spam_control_undo_ui.py'
      - '.github/workflows/apply-spam-control-undo-ui.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/spam-families
          fetch-depth: 1
      - name: Apply guarded patch
        run: |
          set -euo pipefail
          python3 .github/scripts/apply_spam_control_undo_ui.py
      - name: Commit patch if needed
        run: |
          set -euo pipefail
          if git diff --quiet -- app/src/main/java/eu/faircode/email/ActivitySpamFamilyLab.java; then
            echo 'No patch commit needed'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/eu/faircode/email/ActivitySpamFamilyLab.java
          git commit -m 'Add undo and learning reset to Spam Control'
          git push origin HEAD:feature/spam-families
